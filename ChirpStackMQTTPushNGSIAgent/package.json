{
  "name": "chirpstakdownlinh",
  "version": "1.0.0",
  "description": "Send payload to device when Orion entity change",
  "main": "index.js",
  "scripts": {
    "test-brk": "mocha --inspect-brk=0.0.0.0 --recursive",
    "test": "mocha --recursive",
    "debug": "node --inspect=0.0.0.0 index.js",
    "debug-brk": "node --inspect-brk=0.0.0.0 index.js",
    "onetest": "mocha --recursive $TEST",
    "start": "node index.js"
  },
  "author": "Marc Despland",
  "license": "Apache-2.0",
  "dependencies": {
    "async-mqtt": "^2.6.1",
    "axios": "^0.21.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^7.1.1"
  }
}
